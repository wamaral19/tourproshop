{
  "name": "tourproshop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "predev": "npm run generate",
    "dev:full": "SITE_MODE=full next dev",
    "predev:full": "SITE_MODE=full npm run generate",
    "build": "npm run generate && next build",
    "start": "next start",
    "lint": "eslint",
    "preview": "npm run cf-typegen && opennextjs-cloudflare build && node scripts/prune-dark-assets.mjs && opennextjs-cloudflare preview",
    "preview:full": "npm run cf-typegen && SITE_MODE=full SITE_PREVIEW=true opennextjs-cloudflare build --env preview && SITE_MODE=full node scripts/prune-dark-assets.mjs && SITE_MODE=full SITE_PREVIEW=true opennextjs-cloudflare preview --env preview",
    "deploy": "npm run cf-typegen && opennextjs-cloudflare build && node scripts/prune-dark-assets.mjs && opennextjs-cloudflare deploy",
    "deploy:full": "npm run cf-typegen && SITE_MODE=full SITE_PREVIEW=true opennextjs-cloudflare build --env preview && SITE_MODE=full node scripts/prune-dark-assets.mjs && SITE_MODE=full SITE_PREVIEW=true opennextjs-cloudflare deploy --env preview",
    "upload": "npm run cf-typegen && opennextjs-cloudflare build && node scripts/prune-dark-assets.mjs && opennextjs-cloudflare upload",
    "cf-typegen": "wrangler types --env-interface CloudflareEnv cloudflare-env.d.ts",
    "audit:dark": "node scripts/audit-dark-players.mjs",
    "generate": "node scripts/generate-site-mode.mjs && node scripts/generate-player-image-map.mjs && node scripts/generate-neutral-media.mjs && node scripts/check-lockdown-coverage.mjs"
  },
  "dependencies": {
    "next": "16.2.4",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@opennextjs/cloudflare": "^1.19.6",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.4",
    "tailwindcss": "^4",
    "typescript": "^5",
    "wrangler": "^4.88.0"
  }
}
